#include "sevenSegment.h"
#include "pushButton.h"

int main(void)
{
    
    
    while(1)
    {
        detectButton();
    }
    
    return 0;
}


/*
for(int j = 1; j <= 4; j++)
{
    for(int i = 0; i <= 9; i++)
    {
        displayNumber(j, i);
        _delay_ms(500);
    }
}
 */